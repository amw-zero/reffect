/*
let testReducer = () => {
  let state = { Effect.count: 1 };
  let (nextState, _) = reducer(state, IncrementComplete(2));

  let message = 
    switch(nextState.count) {
    | 2 => "Passed"
    | _ => "Failed"
    };

  Js.log(message);
};
*/